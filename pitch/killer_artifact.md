# Killer Artifact

Proof phrase: 1 NO receipt names wallet and hashes

## Why It Works

A judge can see the phrase on the live receipt path: the refused action saves one `NO` receipt with wallet address, role, reason, `policyHash`, `calldataHash`, `shockHash`, and `proofHash`, then links the same record to Arbitrum Sepolia proof.
